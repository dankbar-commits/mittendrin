/**
 * Not wired to any current feature — the app has no listing or search of
 * existing events, only a single draft being built and submitted (see
 * README, "Out of scope"). Left as a placeholder matching the requested
 * file scaffold rather than omitted, but intentionally empty: inventing a
 * fake event card here would invent product scope nobody asked for.
 */
export default function EventCard() {
  return null;
}
